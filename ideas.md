# AIME 评测仪表盘设计构思

## 项目背景
为 AIME vs 竞品 AI 助手的对比评测报告构建可视化 Web 仪表盘，展示 58 个投资场景问题的评测结果。

---

<response>
<text>
## 方案一：数据新闻风（Data Journalism Aesthetic）

**设计运动**：受 The Pudding、Bloomberg Visual Data 等数据新闻媒体启发，强调"数据即叙事"。

**核心原则**：
1. 叙事驱动 —— 数据不只是展示，而是讲述一个故事
2. 克制留白 —— 大面积留白让数据呼吸
3. 信息密度与可读性平衡 —— 每个视觉元素都有明确的信息职责

**色彩哲学**：以纯白/极浅灰为底色，AIME 使用深靛蓝（#1B2A4A）作为主色，竞品使用暖灰橙（#C4956A）作为对比色。胜负用深绿（#2D6A4F）和柔红（#D64045）标注。整体色调沉稳、专业，传递金融级信任感。

**布局范式**：垂直滚动长页面叙事结构，类似 scrollytelling。每个章节占据一个视口高度，通过滚动触发数据动画。侧边导航提供章节锚点。

**签名元素**：
- 大号衬线标题与小号无衬线正文的强烈对比
- 数据卡片使用极细边框 + 微阴影，悬浮感

**交互哲学**：滚动即探索，hover 即深入。鼠标悬停在数据点上时展开详细信息，滚动触发渐进式数据揭示。

**动画**：滚动进入视口时，数字从 0 计数到目标值（duration 800ms），图表从左到右渐进绘制。使用 IntersectionObserver 触发。

**字体系统**：标题使用 Playfair Display（衬线），正文使用 DM Sans（无衬线），数据使用 JetBrains Mono（等宽）。
</text>
<probability>0.06</probability>
</response>

<response>
<text>
## 方案二：终端/黑客美学（Terminal Aesthetic）

**设计运动**：受 Bloomberg Terminal、Retool、Linear 启发的深色数据密集界面。

**核心原则**：
1. 信息密度最大化 —— 一屏展示尽可能多的有效数据
2. 功能至上 —— 每个像素都服务于数据传达
3. 专业感 —— 面向金融从业者的严肃工具感

**色彩哲学**：深色背景（#0F1117）配合高对比度数据色。AIME 使用电光绿（#00E5A0），竞品使用琥珀色（#FFB224）。状态色：胜-绿、负-红、平-蓝灰。整体传递"专业交易终端"的视觉语言。

**布局范式**：多面板 Dashboard 布局，左侧固定导航栏，右侧为可配置的网格面板。类似 Bloomberg Terminal 的多窗格信息密度。

**签名元素**：
- 数据表格使用等宽字体 + 交替行高亮
- 实时闪烁的状态指示灯

**交互哲学**：键盘优先，支持快捷键导航。点击即展开，双击即全屏。面板可拖拽调整大小。

**动画**：极简动画，仅在状态变化时使用 150ms 的 fade/slide。数字变化使用打字机效果。拒绝花哨动画，一切服务于效率。

**字体系统**：全局使用 IBM Plex Mono（等宽），标题加粗，正文常规。数据密集区域使用 11px 紧凑排版。
</text>
<probability>0.04</probability>
</response>

<response>
<text>
## 方案三：瑞士国际主义 + 金融科技融合（Swiss Fintech）

**设计运动**：瑞士国际主义排版（Swiss International Typographic Style）与现代金融科技 UI 的融合。受 Stripe Dashboard、Vercel、Notion 启发。

**核心原则**：
1. 网格秩序 —— 严格的 8px 网格系统，所有元素对齐
2. 层次清晰 —— 通过字重、大小、颜色建立明确的信息层级
3. 功能美学 —— 美来自于秩序和精确，而非装饰

**色彩哲学**：浅色模式为主。背景使用极浅的蓝灰（#F8FAFC），卡片纯白。AIME 品牌色使用深蓝紫（#4F46E5 → Indigo 600），竞品使用中性灰（#6B7280）。数据可视化使用 Indigo 渐变色阶。胜负使用翡翠绿（#059669）和玫瑰红（#E11D48）。整体传递"精确、可信赖、现代"的感受。

**布局范式**：左侧持久侧边栏导航 + 右侧内容区。内容区使用卡片网格系统，12 列响应式布局。顶部有面包屑导航和全局筛选器。

**签名元素**：
- 卡片使用 1px 边框 + 极浅阴影（shadow-sm），hover 时提升阴影层级
- 数据指标使用超大字号（48px+）配合小号标签的强烈对比

**交互哲学**：直觉操作，零学习成本。Tab 切换分类，下拉筛选条件，卡片点击展开详情。一切遵循用户已有的 SaaS 使用习惯。

**动画**：
- 页面切换使用 200ms ease-out fade + translateY(4px)
- 卡片 hover 使用 150ms shadow 提升 + translateY(-1px)
- 图表入场使用 600ms 的渐进绘制
- 数字使用 400ms 的 count-up 动画
- 筛选切换使用 180ms 的内容 crossfade

**字体系统**：标题使用 Plus Jakarta Sans（几何无衬线，现代感强），正文使用 Inter（可读性极佳），数据/代码使用 Geist Mono。字重层级：700（标题）→ 500（小标题）→ 400（正文）。
</text>
<probability>0.08</probability>
</response>

---

## 选定方案：方案三 - 瑞士国际主义 + 金融科技融合（Swiss Fintech）

**选择理由**：
1. 最适合评测报告的专业定位 —— 清晰的信息层级让 58 个 Case 的数据一目了然
2. 卡片网格系统天然适合展示对比评测结果
3. 浅色模式 + Indigo 主色调传递可信赖的金融科技感
4. 侧边栏导航支持在概览、明细、结论之间快速切换
5. 动画克制但精致，不会分散对数据的注意力
